server {
    listen 80;
    server_name chinese-porcelain-art.com www.chinese-porcelain-art.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name chinese-porcelain-art.com www.chinese-porcelain-art.com;

    ssl_certificate /etc/letsencrypt/live/chinese-porcelain-art.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/chinese-porcelain-art.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    client_max_body_size 4G;

    location /media/ {
        alias /app/media/;
    }

    location / {
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Host $host;
        proxy_redirect off;
        proxy_pass http://web:8000;
    }
}
